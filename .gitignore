.DS_Store
**/.DS_Store
node_modules/
*.log

# Secretos / entorno (el token de CAPI NUNCA se sube)
.env
.env.*
!.env.example
# Nota: .htpasswd SÍ se versiona (solo contiene el hash bcrypt, no la clave en claro)
# para que Dokploy lo incluya al construir la imagen.
